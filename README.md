# Docbook Skeleton Document

A skeleton set of files and a Maven-based build for a new Docbook document.  By default, the build supports the following output formats:

* PDF
* Docbook ebook format (iPad, Android)
* HTML single page
* HTML multi-page


